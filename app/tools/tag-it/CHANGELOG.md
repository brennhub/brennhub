# 태그잇 (Tag-it) CHANGELOG

## 0.7.1 — 안전 패턴 보강 (형용사 관형형 + 조사/종결) (2026-05-30)

mecab-ko-dic 명사 충돌 실측 기반 — **명사와 거의 안 겹치는 패턴만** 규칙화(맹신 경계).
충돌 수: 적인=3(목적인 등 copula 잔재)/로운=0/스러운=0/는다=0/에서부터=0 → 안전 확인.

- **형용사 관형형 접미**(`KO_VERB_ENDINGS`): `~적인/~로운/~스러운` 추가 → 객관적인/혁신적인/
  공식적인/새로운/날카로운/사랑스러운 제거. ⚠️ 바ㄹ ~인/~한(충돌 1483/327: 노인·법인·북한)은
  **절대 추가 금지**(명사 학살) — 별도 task에서 명사 사전 보호와 함께만.
- **조사 보강**(`KO_JOSA`): `에서부터`/`이나마`.
- **종결어미 보강**(`KO_VERB_ENDINGS`): `는다`(먹는다 류).

검증: EBTS 강도5 1729→1708(적인/로운 노이즈 21 정리, 잔존 0), 실제 명사 보존(법인/외국인/
사단법인/보고서/투자금/통장…), 3포맷 동일. 사전 불필요·결정론.

— 후속(별도 task, Phase 3): 명사 확률 모델 + NNG 사전(0.83MB) 보호 + weights.ts 부록.
  사전 O=100%·하한 85% / 사전 X=50%·강차감, 규칙 가중치=명사충돌 역수. 큰 변경이라 분리.

## 0.7.0 — 필터 재설계: 세 신호 기반 명사 판별 (2026-05-30)

blacklist 한계(무한 한국어 활용 못 따라감) → **데이터 기반 신호로 명사 판별**(AI 0, 결정론).
실파일(EBTS.pptx 1843 후보)로 "적용 전 계측" 후 구현 — 특히 신호1 순진판(어간=임의 절단,
보고서→보 등 명사 168개 학살) vs 정제판(진짜 어미만, 명사 0 학살, 26개) 차이를 사전 검증.

- **신호1 — 용언 활용 패러다임**(강도 4+): 검증 어미(`KO_INFL_ENDINGS`)만 떼어 어간을 얻고,
  그 어간이 문서에서 용언 활용(`KO_VERBAL_MARKERS`: 하다/되다/었다…)과 함께 나타날 때만 활용형으로
  제거. **임의 절단 금지** → 보고서·투자금·통장·사용자·논리·급여 등 복합/파생 명사 보존.
  기능 용언 어간(`KO_FUNCTIONAL_STEMS`: 있/없/같…)은 빈도 무관 제거, borderline 어간(보=報告)은
  빈출이면 명사로 보존(빈도 가드).
- **신호2 — 인용·종결 어미**(강도 3+): `KO_VERB_ENDINGS`에 라고/다고/았다고/라며 등 + 단독형
  stopword. 받았다고/출범했다고 류 제거.
- **신호3 — 1음절 의존명사+조사**(강도 5): 것은/것을/것이 등 제거. 진짜 1음절 명사(돈을)는 보존.
- **URL/웹 토큰 제거**(강도 5): 슬라이드 하이퍼링크 URL(`https?://…`)을 토큰화 전 제거 →
  https/youtube/namu/wiki/news/html 등 소멸.
- **형용사 관형형(~ㄴ/~인 "객관적인")은 보류**(BACKLOG): 외국인/노인/법인 등 통짜 명사 학살
  위험 — 실데이터로 재확인. "객관적인 몇 개 잔존 < 명사 사망".
- 강도·빈도 분리(§0.6.2)는 그대로 — "후보 양(1843 long tail)"은 빈도 문제로 이 작업과 분리.

검증: 실파일에서 명사 보존 30/30, 활용형/종결/URL/1음절 노이즈 제거, 강도 단조성, 3포맷 동일.
1843→1729(114 정리), 처리 39ms.

## 0.6.2 — 추출 정밀화: 명사 어간 추출 + 강도·빈도 분리 (2026-05-29)

실파일 계측으로 3개 독립 원인 확정 후 우선순위 #2→#3→#1 수정.

- **#2 명사 어간 추출(핵심)**: 용언("도출하였다")을 통째 버리던 것을 → 접미 용언(하/되/시키 계열)을
  떼어 명사 어간 복원("도출"/"분석"/"평가"). `KO_NOUN_VERB_SUFFIXES` + `extractNounStem`.
  - 과분리 3중 가드: 남는 어간 한글 ≥2일 때만 분리(위하여→위 미분리), 접미는 하/되/시키 한정
    (먹었다 등 일반 동사 미분리), 1음절-stem 명사(북한/포함/결함/이해) 보존.
  - 조사 제거보다 **먼저** 실행 — "포함하는"의 "는"이 조사로 떨어져 "포함하"가 되기 전에 "포함" 복원.
  - 효과: 직전 문단-join 수정으로 역으로 사라졌던 명사 부활 + 용언에 갇힌 명사 대량 복원(recall↑).
- **#3 강도-빈도 축 분리**: `FilterProfile`에서 `minFreqFloor` 제거. 강도 4·5의 floor=2가 1회 등장
  명사를 전멸시키던 문제 해소 → 강도는 "명사 판별 엄격도"만, 빈도 컷은 사용자 `minFreq` 옵션만.
  강도 5에서도 1회 명사 생존. (단조성은 필터 strictness로 유지)
- **#1 사전 안전 보강**: 복합조사 `으로부터`/`로부터` `KO_JOSA` 추가, 단독 의존명사 `번째` stopword 추가.
  형용사 관형형(~ㄴ/~인 "객관적인")은 명사 오삭제 위험 커 보류(BACKLOG).

검증: "도출하였다→도출" 복원·과분리 가드·1음절 보존·일반동사 미분리, 강도 5 1회 명사 생존+사용자
minFreq 작동, 로부터/번째 제거·명사 오삭제 0, **3포맷 동일 토큰** 스모크 통과(임시·삭제). 단계별 build+lint.

## 0.6.1 — 추출 버그 수정: 문단 단위 join (pptx/xlsx 토막 복원) (2026-05-29)

**증상**: pptx(및 리치텍스트 xlsx)에서 강도 5인데도 조사·어미 노이즈(에서/로부터/하지/
도출되/돈을/객관적인…)가 대량 후보로. **원인**: `extractByTag`가 런(`<a:t>`/`<w:t>`/`<t>`)을
공백으로 join → PowerPoint가 단어 중간을 여러 런으로 쪼개면 단어가 토막나고, 분리된
조사/어미 조각이 "어근에 붙은 조사/어미"를 가정한 필터를 빠져나감. (엔진은 포맷 무관 동일,
본문 추출의 토큰 경계 손상이 원인 — 코드 경로 분기 아님.)

**수정** (`office.ts`, 방향 1 = 문단 단위 join):
- `extractByTag` → `extractParagraphs(xml, paraTag, runTag)`. 문단/셀(`<w:p>`/`<a:p>`/`<si>`)
  **안의 런은 공백 없이("") 연결**(단어 복원), **문단·셀 사이만 공백**.
- 근거: OOXML에서 단어 띄어쓰기는 항상 런 내용 안의 문자(xml:space="preserve" 포함) →
  ""로 이어도 진짜 공백 보존 + 토막만 복원.
- 줄바꿈·탭(`<w:br/>`/`<w:tab/>`/`<a:br/>`)은 run이 아니라 공백 런으로 치환 → 단어 병합 방지.
- run 태그만 추출 유지(`<w:instrText>` 등 비가시 필드 제외) → **docx 회귀 0**(단일런 불변,
  여러 런 문단은 복원되어 개선).
- 3포맷 동일 적용 → 추출 결과 포맷 무관 정규화("문서는 문서다").

**검증**: 동일 의미 문장을 docx/pptx(런 토막)/xlsx(리치텍스트 셀)로 만들어 추출 원문·토큰이
**완전 동일**함을 스모크로 확인. 진단 노이즈(에서/로부터/도출하/였다) 소멸, 명사(매출/고객/
분석/보고서/도출) 보존, 줄바꿈·탭 병합 없음. build + lint 통과.

## 0.6.0 — 포맷 확장: xlsx/pptx 추가 (2026-05-29)

.docx 전용 → **오피스 문서 3종(Word·Excel·PowerPoint)**. OOXML core.xml의 keywords 위치가
동일해 읽기·쓰기·재포장·무결성·기존 keywords 프리셋은 그대로 공유, 본문 추출만 포맷별 분기.

- **`docx.ts` → `office.ts` rename + 포맷 디스패치**: `detectFormat`/`isOfficeFile`/`unzipOffice` +
  `readText(entries, format, scope)`.
- **xlsx**: `xl/sharedStrings.xml`의 `<t>`(리치텍스트 포함) + 시트 inline string(`<is><t>`).
  한자 phonetic(`<rPh>`) 제외. ⚠️ 빈도는 sharedStrings 기준(고유 문자열) — 실제 셀 횟수와 다를 수 있음(BACKLOG).
- **pptx**: `ppt/slides/slideN.xml`의 `<a:t>`, **슬라이드 번호 자연 정렬**(slide2 < slide10).
  노트·머리/바닥글 제외(BACKLOG).
- **무결성 동일**: 본문 XML(word/document·xl/*·ppt/slides/*) 미수정, core.xml 한 엔트리만 교체 —
  세 포맷 동일 코드 경로. 다운로드 MIME 포맷별 매핑, 파일명·확장자 원본 유지.
- 통합: 업로드 `accept=".docx,.xlsx,.pptx"`, i18n(ko/en) 카피·도구 설명 "오피스 문서"로 갱신,
  `limitNotDocx`→`limitNotOffice`. 추출 엔진·강도·세그먼트 등은 포맷 무관 공유("문서는 문서다").
- 검증: 합성 docx/xlsx/pptx 추출·자연정렬·본문 바이트 불변·core.xml 교체 스모크 통과.

## 0.5.0 — UI 전체 재설계 (시각 위계·밀도·세그먼트·마감) (2026-05-29)

기능 회귀 0, 디자인·레이아웃·시선 흐름만 개선 (refined minimalism, brennhub zinc 유지).
단계별(P3→P1→P2→P4) 구현 + 단계마다 빌드 통과.

- **P3 세그먼트 컨트롤**: 필터 강도 슬라이더 → 가로 5칸 세그먼트(`segmented.tsx`, 강도·모드 공용).
  칸 통째 클릭(Fitts) · radiogroup + ←→ 키보드 · 현재 단계명 + 한 줄 힌트.
- **P1 시각 위계 3층**: ① 입력(업로드) → ② 추출 설정(접힘, 강도·범위·최소빈도) → ③ 주 작업공간(결과).
  모드(자동/수동)는 추출 설정에서 빼 **작업공간 헤더(칩 바로 위)에 상시 노출** — 근본 갈림길이라 발견성 우선.
  작업공간 상시 노출 + 빈 상태 안내.
- **P2 후보 밀도**: 칩면 빈도 숫자 제거(hover 툴팁으로만) · 후보 구역에 **칩 검색**(실시간 부분일치) ·
  기본 노출 30 → 20 · 더보기/검색 병행.
- **P4 마감**: 채택=채움 / 후보=외곽선(투명) 색+형태 이중 구분(색맹 안전) · 옵션·모드 변경 시
  "갱신됨" 마이크로 펄스(절제) · 빈 상태/검색없음 문구.

## 0.4.0 — 추출 품질 2차 보강 + 필터 강도 슬라이더 (2026-05-29)

**A. 잔여 노이즈 (형태 기반, 문서 무관)**
- 조사 제거를 영문/숫자 어근에도 적용(`tokenize.ts`): `gd에→gd`, `70과→70`, `preload만으로→preload`.
  어간 유효성 = 한글 ≥2음절 또는 영문·숫자 포함. 스택 조사 반복 제거. 한글 1음절 어근(회의/수로)은 가드로 보존.
- 활용형 어미 보강(`stopwords.ko.ts`): 되는/하는/지는/되며/된 등(관형형·피동). `~ㄹ` 관형형(건드릴)은
  명사 충돌(드릴·소설·수단)로 결정론 안전 제거 불가 → 추가 안 함(명사 보존 우선).

**B. "명사 위주" 토글 → 5단계 "필터 강도" 슬라이더**
- 1 관대 ↔ 5 엄격. 강도→필터 프로파일(`extract.ts` PROFILES): 강도별로 숫자영문단편/조사·활용형·세는단위/
  약어감점/최소빈도 floor/약어제외를 점증 적용. 기호·비문자·기능어 제거는 전 강도 항상.
- 기본값 3(균형, 기존 동작 수준). 슬라이더 + 현재 단계명 + 한 줄 힌트(무엇을 거르는지) i18n(ko/en).
- removeJosa 토글을 강도에 흡수(별도 토글 제거). minFreq 스텝퍼는 유지(강도가 내부 floor 추가).
- localStorage 옵션 스키마 v1→v2(구버전 값 무시, 기본 강도 3로 1회 리셋).

## 0.3.0 — 추출 품질 보강 (2026-05-29)

dev 실사용 결과 "명사 위주 ON"인데도 활용형·숫자결합·기호파편이 후보로 노출되던 문제.
문서 종류를 가리지 않는 **형태 기반 보편 규칙**으로 노이즈 제거 (코드/산문 분기 없음).

- **활용형 어미 확장**(`stopwords.ko.ts` `KO_VERB_ENDINGS`): 이라/으면/하면/해야/하지/했던/는데/라는 등
  2글자+ 위주 추가(명사 위주 ON일 때 제외). 1글자는 충돌 적은 `던`만 — 이미지·편지·측면·신라 등 명사 보호.
- **형태 노이즈 필터 3종**(`extract.ts`, 명사위주 무관 항상 적용):
  - 기호·비문자 토큰(i→, s×3s) — 유니코드 글자 기준이라 fallback 언어 안전.
  - 숫자+영문 단편(5px, 0s, F1) — 한글 포함 토큰(AI보고서·Q3실적)은 면제.
  - 숫자+세는단위(1줄/2가지/3번/1개) — 시간·물리 단위(30분·2개월)는 보존.
- **약어 약한 감점**(`extract.ts`): gd/dps/int 등은 제외하지 않고 점수 -1 — 빈출 약어(MRI 등)는 상위 유지.
- 어미가 붙은 활용형은 **어간 복원이 아니라 제외**(결정론·안전 우선). "후보에서 골라담기" 구조라 충분.

## 0.2.0 — 칩 직관성 UI 개선 (2026-05-29)

- 핀(보호) UI 숨김(2차 부활), 칩 영역 "선택한 태그"/"후보" 2구역 분리, 일괄 선택(전체/전체해제/상위N).

## 0.1.0 — MVP 1차 (2026-05-29)

신규 도구 추가. .docx 키워드 칩 추출·편집·기록 (클라이언트 전용, AI 미사용).

**.docx 처리 (`lib/tag-it/docx.ts`)**
- fflate 기반 zip 해제 → 본문(`word/document.xml`) + 표 텍스트 추출.
- core.xml `<cp:keywords>` 읽기·쓰기. **core.xml 한 엔트리만 교체, 나머지 바이트 보존** → 본문 무결성 유지 (손상 경고 방지).
- 여러 .docx 일괄 .zip 묶음(level 0, 재압축 회피).

**추출 엔진 (`lib/tag-it/`)**
- 토큰화 + 조사 longest-match 제거. **과도제거 가드**: 떼고 남는 어간이 2음절 미만이면 원형 보존("회의→회" 방지).
- 점수화(빈도 + 제목 영역 가산점) + 명사 위주 네거티브 필터(용언 어미 제외) + 중복 병합.
- 한국어·영어 전용 불용어 3층 사전(코드와 분리, 단어만 추가 가능). 그 외 언어 fallback.

**UI**
- 칩 캔버스: 후보/채택/보호/숨김 상태 모델, 본체클릭=채택·핀=보호·×=삭제·직접입력.
- 자동/수동 모드 토글 (모드 전환 시 사용자 칩 보존).
- 고급 옵션(조사 제거·명사 위주·읽기 범위·최소 빈도) + localStorage 영속.
- 점진 노출(상위 30 + 더보기). 업로드 한계 검증(파일 5개/8MB/15MB, 어느 한계인지 구분 경고).
- 복수 파일: 공통 트레이(일괄 적용) + 파일별 카드(상태 배지) + 개별/일괄 다운로드.

**brennhub 통합**
- registry(`status:"live"`, `hasPage:true`) + page + 문서 3종.
- i18n `tagIt` namespace (ko/en) + tools + feedback(`toolTagIt`) 6곳 배선.
- 의존성 `fflate` 추가 (브라우저 zip — 자체 구현이 유지보수성 해침, 기획서 §3.1 명시 예외).
