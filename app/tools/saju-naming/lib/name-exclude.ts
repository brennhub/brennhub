/**
 * 추천 부적합 한자 필터 — Task 39-C 추천 품질 가드.
 *
 * recommend 풀(inname_ok=1)은 법적 사용 가능 인명용 한자지만 의미 불량(卵 알·危 위태)
 * ·희귀 벽자(㔕)가 섞여 의미-blind 점수만으로 상위 등극(dev 검증 2회: 金犴危/金卵㔕).
 * frequency 실데이터 미가용(39-C 정찰) → 경량 가드:
 *   1. 희귀 블록 — CJK URO 밖(확장 A/B) 제외.
 *   2. 부정 의미 키워드 — 훈에 죽을·위태·무너질 등 → 제외.
 *   3. 명시 블랙리스트 — 키워드 충돌 회피로 못 잡는 명백 부정·부적합 한자.
 *   4. 작명 부적합 의미군(39-C 후속) — 숫자·대명사·접속·기능어 등 이름에 안 쓰는 뜻
 *      (六 여섯·全 온전·同 한가지·共 함께류). frequency 티어가 벽자는 demote했으나 점수
 *      포화 동점에서 흔한 비-작명 한자가 상위 등극(蘇 추천 시뮬 발견) → 의미 가드로 차단.
 *
 * 키워드/블랙리스트는 **풀 9,055 의미(훈) 데이터 스캔으로 도출**(추측 X) 후 도메인
 * 선별. 충돌 회피 원칙:
 *   - 다음절·저모호 부정 훈만 키워드 (예 "위태"·"무너질").
 *   - 음·타훈 충돌 어휘는 키워드 배제하고 특정 한자만 블랙리스트:
 *       "병"→炳秉柄兵 충돌 → 病疾疫 등 char / "거짓"→誕(낳을)·眠(잘) 충돌 → 僞 char /
 *       "귀신"→神 충돌 → 鬼 char / "똥"→便 충돌 → 屎糞 char / "낮을"→低 충돌 → 卑庳 char /
 *       "험할"→試(시험할) 충돌 → 險嶮 char / "독할"→督 충돌 → 酷 char /
 *       "사나울"→猛 충돌 → 暴 char / "다칠"→創(비롯할) 충돌 → 傷 char /
 *       "벌레"→昆(맏) 충돌 → 蟲虫 char / "마귀"→까마귀·사마귀 충돌 → 鬼 char.
 *
 * best-effort 휴리스틱 — 완전 의미/어감 평가는 Task 45 AI 어감(Premium).
 */

export interface ExcludeEntry {
  character: string;
  meaning: string | null;
}

/**
 * CJK URO(U+4E00~U+9FFF) 밖 = 희귀 블록.
 * 확장 A(U+3400~)·확장 B 이상(U+20000~)·비표준(plane 10/15) 모두 포함.
 */
export function isRareBlock(character: string): boolean {
  const cp = character.codePointAt(0);
  if (cp === undefined) return false;
  return cp < 0x4e00 || cp >= 0x20000;
}

/**
 * 부정 의미 훈 키워드 — 풀 의미 스캔 도출(다음절·저모호만, 음·타훈 충돌 어휘 배제).
 * 사망·질병/장애·재앙·악/추함·범죄·슬픔·요괴·부패·어리석음·궁핍·음란·태만·부정 동물 등.
 */
export const NEGATIVE_MEANING_KEYWORDS: readonly string[] = [
  // 사망
  "죽을", "죽일", "주검", "시체", "송장", "무덤",
  // 질병·장애
  "학질", "부스럼", "종기", "벙어리", "귀머거리", "곱사", "절뚝", "절름",
  // 재앙·위험
  "위태", "재앙", "재난",
  // 악·흉·추함
  "흉할", "흉악", "사악", "악할", "추할", "천할", "더러울", "모질",
  // 범죄·원한
  "도둑", "도적", "훔칠", "속일", "헐뜯", "원망", "원수",
  // 슬픔·탄식
  "슬플", "한탄",
  // 요괴
  "요괴", "도깨비",
  // 부패·소멸
  "썩을", "곰팡이", "구더기", "시들", "무너질", "망할",
  // 어리석음·어두움
  "어리석을", "어두울",
  // 궁핍
  "가난", "주릴",
  // 음란·태만
  "음란", "음탕", "게으를",
  // 부적합 동물(지지·해충 포함)
  "돼지", "뱀",
];

/**
 * 명시 제외 — 키워드 충돌 회피로 못 잡는 명백 부정·부적합 한자 (풀 스캔 확인).
 * 각 한자 verbatim 의미는 docs/CHANGELOG·보고 참조.
 */
export const EXPLICIT_EXCLUDE: ReadonlySet<string> = new Set([
  // 비부정-부적합 동물·사물 (키워드 회피군)
  "卵", // 알
  "蛇", // 뱀
  "鼠", // 쥐
  "蟲", // 벌레
  "虫", // 벌레
  "豚", // 돼지
  // 질병 ("병" 음 충돌 → char)
  "病", // 병 병
  "疾", // 병 질
  "疫", // 전염병 역
  "瘟", // 돌림병 온
  "癘", // 나병 려
  "癩", // 나병 라
  // 배설 ("똥/오줌" 便 충돌 → char)
  "屎", // 똥 시
  "糞", // 똥 분
  "尿", // 오줌 뇨
  // 낮음·험준 ("낮을" 低 / "험할" 試 충돌 → char)
  "卑", // 낮을 비
  "庳", // 낮을 비
  "險", // 험할 험
  "嶮", // 험할 험
  // 귀신·거짓 ("귀신" 神 / "거짓" 誕·眠 충돌 → char)
  "鬼", // 귀신 귀
  "僞", // 거짓 위
  // 가혹·포악·상해 ("독할" 督 / "사나울" 猛 / "다칠" 創 충돌 → char)
  "酷", // 독할 혹
  "暴", // 사나울 포
  "傷", // 다칠 상
  // ─────────────────────────────────────────────────────────────────────
  // C-2-pre (2026-06-06) 풀 상단 ws≤2 sweep 추가 — 11자.
  //
  // 배경: dev 회귀(외숙모 yongsin [금,수])에서 char2가 了 100% 독점 → char2
  //       다양성 가드 도입 후 60%로 감소, 단 등장 char2(了·刀·口) 모두 인명
  //       부적합. 원인 = D1 풀 정렬(freq DESC + stroke ASC)에서 ws≤2 글자가
  //       풀 최상단 + ws≤2 한자 다수가 부정/도구/단순 의미. 다양성만으론
  //       한계 → 풀 자체에서 부적합 글자 제거 필요.
  // sweep 범위: staged-hanja.json + staged-won-stroke.json 합치 ws≤2 = 19자.
  // 추가 대상 = 명백 부적합 11자 (보존 5자: 乙·丁 천간 / 儿·力 긍정 / 刁 애매
  //   → 과잉 제외 방지 기준 = "명백 부정/폭력/도구/단순명사/기능어만 추가").
  // 키워드 추가 X — "마칠"→終(끝나다)·"칼"→劍(검) 등 오탐 위험. char 명시만.
  // ws=3 sweep은 본 task 효과 검증 후 별도 task.
  "乂", // 벨 예 — 베다(자르다), 폭력 동사
  "乃", // 이에 내 — 기능어 (접속·전치)
  "了", // 마칠 료 — 끝/마침 (dev 회귀 외숙모 char2 60% 독점 글자)
  "人", // 사람 인 — 일반명사, 작명 어감 빈약
  "入", // 들 입 — 단순 동사
  "冂", // 멀 경 — 부수, 단독 사용 매우 드뭄
  "几", // 안석 궤 — 일반 사물(책상·작은 탁자)
  "刀", // 칼 도 — 폭력 도구 (dev 회귀 등장)
  "匕", // 비수 비 — 무기(단도)
  "匸", // 감출 혜 — 부수, "감출" 부정 함의
  "卜", // 점 복 — 점복·점성, 작명 의미 부적합
  // 보존(의도적 미포함): 乙(천간·이름 빈번)·丁(천간·이름 빈번)·儿(어진 사람)·
  //                     力(힘)·刁(조두, 의미 모호 → 애매 시 보존).
]);

/**
 * 화이트리스트 — 키워드 오탐이나 작명상 양호해 통과시키는 예외 한자.
 * 다의어가 부정 훈을 함께 가져 키워드에 걸리지만 긍정 의미가 주된 경우.
 */
export const WHITELIST: ReadonlySet<string> = new Set([
  "憧", // 동경할/어리석을 동 — "어리석을" 오탐 보호 (憧憬=동경, 긍정)
]);

/**
 * 작명 부적합 의미군 키워드 (39-C 후속) — 숫자·기능어 등 이름에 안 쓰는 뜻.
 * 풀 9,055 의미 스캔으로 **오탐 0 검증된 키워드만**. 충돌 어휘는 키워드 배제 + EXPLICIT char:
 *   "아홉"→玖(옥돌)·申 / "서른"→鈞 / "온전"→㦃(온전한 덕) / "어찌"→巨(클) / "마땅"→宜·當 /
 *   "이를"→至·致·早 / 단위(되·말·자·치·섬·길)→升(오를)·石·馬·任 등 충돌 → 키워드 미사용.
 * 매칭 한자(전부 비-작명): 六鍰·五伍·八咫捌·七柒漆·卄廿·仝同·各·全囫·凡.
 */
export const NON_NAME_MEANING_KEYWORDS: readonly string[] = [
  "여섯", "다섯", "여덟", "일곱", // 수사
  "스물", // 卄廿
  "한가지", // 仝同
  "각각", // 各
  "온전할", // 全囫 ("온전한 덕" 㦃 보호 — "온전" 미사용)
  "무릇", // 凡
];

/**
 * 작명 부적합 명시 char — 수사·기능어 중 키워드가 못 잡거나(타훈 충돌) 직접 지정이 안전한 것.
 * char 직접 매칭이라 충돌 0. 보호 대상(相 서로·玖 옥돌·鈞·申·巨 클)은 미포함.
 * (일반 명사·동사 皇 임금·革 가죽·音 소리 등은 미포착 — best-effort 경계, 작명 빈도 자료 의존 backlog.)
 */
export const NON_NAME_EXPLICIT: ReadonlySet<string> = new Set([
  // 기본 수사 (훈 "한 일/두 이/석 삼/넉 사/아홉 구/열 십/서른 삽" — 키워드화 시 타훈 충돌 → char)
  "一", "二", "三", "四", "九", "十", "卅",
  // 대명사·접속·기능어
  "共", // 함께 공
  "又", // 또 우
  "亦", // 또 역
  "咸", // 다 함
  "皆", // 다 개
  "此", // 이 차
  "其", // 그 기
]);

/** 의미(훈)에 부정 키워드 포함 여부. */
export function hasNegativeMeaning(meaning: string | null): boolean {
  if (!meaning) return false;
  return NEGATIVE_MEANING_KEYWORDS.some((k) => meaning.includes(k));
}

/** 의미(훈)에 작명 부적합(숫자·기능어) 키워드 포함 여부. */
export function hasNonNameMeaning(meaning: string | null): boolean {
  if (!meaning) return false;
  return NON_NAME_MEANING_KEYWORDS.some((k) => meaning.includes(k));
}

/**
 * 추천에서 제외할 한자인지. 화이트리스트 우선.
 * 희귀 블록 ∪ 부정 명시 ∪ 부정 의미 ∪ 작명 부적합 명시 ∪ 작명 부적합 의미.
 */
export function isExcludedFromRecommend(entry: ExcludeEntry): boolean {
  if (WHITELIST.has(entry.character)) return false;
  return (
    isRareBlock(entry.character) ||
    EXPLICIT_EXCLUDE.has(entry.character) ||
    NON_NAME_EXPLICIT.has(entry.character) ||
    hasNegativeMeaning(entry.meaning) ||
    hasNonNameMeaning(entry.meaning)
  );
}
